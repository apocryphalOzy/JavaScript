// Global (name)
  // Local (name)
    // Local
  // Local 

let name = 'Jeremy';

if (true) {
    let name = 'Johnathy'
    if (true) {
        name = 'Jen'
        console.log(name)
    }
}

if (true) {
    console.log(name)
}